{
  "name": "nodepy-docs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "!mkdocs build",
    "serve": "!mkdocs serve",
    "gh-deploy": "!mkdocs gh-deploy"
  },
  "engines": {
    "nodepy": ">=0.0.19"
  },
  "python-dependencies": {
    "Pygments": ">=2.2.0",
    "mkdocs": ">=0.16.1",
    "mkdocs-bootstrap": ">=0.1.1",
    "setuptools": ">=34.3.3"
  }
}